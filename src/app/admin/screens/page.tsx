import { PlaceholderSection } from "@/components/nav/PlaceholderSection";

// Owned by the screens unit (M2) — screen CRUD, templates, live preview.
export default function ScreensPage() {
  return <PlaceholderSection title="Screens" />;
}
