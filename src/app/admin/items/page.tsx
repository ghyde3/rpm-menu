import { PlaceholderSection } from "@/components/nav/PlaceholderSection";

// Owned by the items-categories-tags unit — replace this page with the
// real Items list/CRUD UI.
export default function ItemsPage() {
  return <PlaceholderSection title="Items" />;
}
