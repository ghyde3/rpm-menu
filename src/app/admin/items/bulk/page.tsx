import { PlaceholderSection } from "@/components/nav/PlaceholderSection";

// Owned by the changes-bulk-ops-and-revert unit (M3) — bulk availability/
// category/tag/price-adjust preview→apply flow.
export default function BulkOpsPage() {
  return <PlaceholderSection title="Bulk Ops" />;
}
