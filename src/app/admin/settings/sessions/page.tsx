import { PlaceholderSection } from "@/components/nav/PlaceholderSection";

// Owned by the settings-api-keys-data-recovery-sessions-qr unit (M3).
export default function SessionsSettingsPage() {
  return <PlaceholderSection title="Settings · Sessions & Security" />;
}
