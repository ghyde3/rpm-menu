import { PlaceholderSection } from "@/components/nav/PlaceholderSection";

// Owned by the settings-branding-menu-behavior unit (M2); the qr/
// sub-route is reserved for the M3 settings-api-keys-data-recovery-sessions-qr
// unit per plan owns_paths.
export default function MenuBehaviorSettingsPage() {
  return <PlaceholderSection title="Settings · Menu Behavior" />;
}
