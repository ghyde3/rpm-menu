import { PlaceholderSection } from "@/components/nav/PlaceholderSection";

// Owned by the settings-venue-users unit (M1).
export default function UsersSettingsPage() {
  return <PlaceholderSection title="Settings · Users" />;
}
