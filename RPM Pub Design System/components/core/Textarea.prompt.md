**Textarea** — multi-line dark field for menu item descriptions in the CMS.

```jsx
<Textarea label="Description" rows={3} value={desc} onChange={e => setDesc(e.target.value)}
  placeholder="Corned beef, kraut, swiss & thousand island on toasted marble rye" />
```
