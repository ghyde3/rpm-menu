**Card** — matte raised panel; the workhorse container for CMS forms and list rows.

```jsx
<Card>Item form fields…</Card>
<Card accent>Featured / active item</Card>
```

`accent` adds a thick red left rule. `padded={false}` for edge-to-edge list rows.
